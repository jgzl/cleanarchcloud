server:
  port: 8102
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/b_sso_2?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    username: root
    password: root
  redis:
    host: 127.0.0.1
    password: 123456

security:
  oauth2:
    client:
      client-id: ssoclient2
      client-secret: ssoclient2
      user-authorization-uri: http://localhost:8010/oauth/authorize
      access-token-uri: http://localhost:8010/oauth/token
      scope: sso
    resource:
      jwt:
        key-uri: http://localhost:8010/oauth/token_key

cleanarch:
  frontend-url: http://localhost:${server.port}/${server.servlet.context-path}