server:
  port: 8101
spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/b_sso_1?useSSL=false&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    type: com.zaxxer.hikari.HikariDataSource
    username: root
    password: root
  redis:
    host: 127.0.0.1
    password: 123456

security:
  upms-url: http://localhost:8010
  frontend-url: http://localhost:${server.port}/${server.servlet.context-path}
  redirect-url: ${security.upms-url}/logout?redirect_url=${security.frontend-url}
  oauth2:
    client:
      client-id: ssoclient1
      client-secret: ssoclient1
      user-authorization-uri: ${security.upms-url}/oauth/authorize
      access-token-uri: ${security.upms-url}/oauth/token
      scope: sso
    resource:
      jwt:
        key-uri: ${security.upms-url}/oauth/token_key